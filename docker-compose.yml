services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: tanit
    ports:
      - "1234:5432"
    volumes:
      - tanit_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d tanit"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://postgres:admin@postgres:5432/tanit
      JWT_SECRET: tanit-docker-secret
      JWT_EXPIRES_IN: 7d
      PORT: 3001
      FASTAPI_URL: http://fastapi:8000
      FRONTEND_ORIGIN: http://localhost:3000
      UPLOAD_DIR: /app/uploads
    volumes:
      - backend_uploads:/app/uploads
    depends_on:
      postgres:
        condition: service_healthy

  fastapi:
    build:
      context: ./ai-service
      dockerfile: Dockerfile
    environment:
      DATABASE_URL: postgresql://postgres:admin@postgres:5432/tanit
      OPENROUTER_API_KEY: ${OPENROUTER_API_KEY:-}
      OPENROUTER_MODEL: ${OPENROUTER_MODEL:-openai/gpt-4o-mini}
      OPENROUTER_HTTP_REFERER: ${OPENROUTER_HTTP_REFERER:-http://localhost:5174}

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_URL: ""
    depends_on:
      - backend

  gateway:
    image: nginx:alpine
    ports:
      - "3000:80"
    volumes:
      - ./gateway/nginx.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - frontend
      - backend

volumes:
  tanit_pg_data:
  backend_uploads:
